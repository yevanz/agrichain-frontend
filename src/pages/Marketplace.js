import React from 'react';
import Card from '../components/Card';

const Marketplace = () => {
  return (
    <div>
      <h2>Marketplace</h2>
      <Card title="Central Hub" content="Browse verified produce listings with filters (type, region, price, availability)." />
      {/* Add listings here */}
    </div>
  );
};

export default Marketplace;
